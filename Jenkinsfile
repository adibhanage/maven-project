def TUT = 0

pipeline {
    agent any
    stages {
        if (TUT == 1) {
            stage('Build') {
                steps {
                    echo "Hello World"
                }
            }
        }
    }
}